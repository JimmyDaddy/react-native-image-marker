---
title: Watermark an image online
description: Add text and a logo to a PNG, JPEG, or WebP image. Adjust layout, opacity, position, rotation, and outline without uploading it.
template: splash
---

import OnlineImageTool from '../../../components/tools/OnlineImageTool.astro';

Add one watermark or repeat it across the image. The built-in sample renders immediately, and your own file never leaves the browser.

<OnlineImageTool tool="watermark" />

## What this tool covers

Use text, the Image Marker logo, or both; switch between a single anchored watermark and a staggered tiled layout; then export PNG or JPEG. For multiple custom layers, blend modes, backgrounds, and generated SDK code, use the [full playground](/playground/).
