---
title: Image watermark Recipe builder
description: Edit and validate reusable React Native Image Marker Recipe JSON, preview it on an image, export it, and copy integration code.
template: splash
---

import OnlineImageTool from '../../../components/tools/OnlineImageTool.astro';

Start from a valid Recipe, change its ordered layers and output settings, then preview the exact Web renderer result.

<OnlineImageTool tool="recipe-builder" />

## Portable, data-driven watermarking

Recipes can contain text templates, ordered text and image layers, conditional visibility, output format, quality, and canvas options. Keep image assets outside the JSON and inject trusted sources at runtime. Imported JSON is validated by the SDK before rendering.
