---
title: Content Credentials inspector
description: Read an existing C2PA manifest from a local image with the official contentauth Web SDK, without uploading or signing the file.
template: splash
---

import OnlineImageTool from '../../../components/tools/OnlineImageTool.astro';

Inspect Content Credentials already embedded in a JPEG or PNG. The official C2PA WebAssembly reader loads only when you start the check.

<OnlineImageTool tool="content-credentials" />

## Read-only provenance inspection

This page displays the manifest store supplied by the file. It does not create a credential, validate a person’s identity, or decide whether the picture is AI-generated. Production signing requires protected credentials and belongs in a trusted native or server environment.
