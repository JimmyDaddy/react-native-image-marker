---
title: 在线给图片加水印
description: 不上传图片，为 PNG、JPEG 或 WebP 添加文字与 Logo，并调整排列、透明度、位置、旋转和描边。
template: splash
---

import OnlineImageTool from '../../../../components/tools/OnlineImageTool.astro';

添加单个水印，或者让文字覆盖整张图。内置示例会直接生成结果，换成自己的文件后也只在浏览器中处理。

<OnlineImageTool tool="watermark" locale="zh-CN" />

## 这个工具能做什么

可以只加文字、同时加 Logo，在单个定位和平铺之间切换，并导出 PNG 或 JPEG。需要多图层、混合模式、文字背景或调用代码时，请使用[完整在线体验](/zh-cn/playground/)。
