---
title: Content Credentials 检查器
description: 使用 contentauth 官方 Web SDK 本地读取图片中的 C2PA manifest，不上传也不签名。
template: splash
---

import OnlineImageTool from '../../../../components/tools/OnlineImageTool.astro';

检查 JPEG 或 PNG 中已经存在的 Content Credentials。只有开始检查后，页面才会加载官方 C2PA WebAssembly 读取器。

<OnlineImageTool tool="content-credentials" locale="zh-CN" />

## 只读的来源信息检查

页面只展示文件提供的 manifest store，不创建凭证、不验证某个人的身份，也不判断图片是否由 AI 生成。生产签名需要受保护的证书和密钥，应放在可信原生环境或服务端。
